name: cooker
description: Пrocery online store

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_i18n: ^0.22.0
  flutter_platform_widgets: ^0.60.2
  bloc: ^6.0.1
  flutter_bloc: ^6.0.1
  json_serializable: ^3.4.0
  kiwi: ^2.0.0
  dio: ^3.0.10
  sticky_headers: ^0.1.8+1
  retrofit: any
  logger: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  retrofit_generator: any
  kiwi_generator: any
  build_runner: any

flutter:
  uses-material-design: true
  assets:
    - assets/flutter_i18n/
